<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.vanskarner.adapters">

    <application
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Adapters"
        tools:ignore="AllowBackup">

        <activity
            android:name="com.vanskarner.adapters.ui.MenuActivity"
            android:exported="false">
            <!--<intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>-->
        </activity>

        <activity
            android:name="com.vanskarner.adapters.ui.search_pagination.SearchPaginationActivity"
            android:theme="@style/Theme.Adapters.FullScreen" />

        <activity
            android:name="com.vanskarner.adapters.ui.simple_pagination.SimplePaginationActivity"
            android:theme="@style/Theme.Adapters.FullScreen" />

        <activity
            android:name="com.vanskarner.adapters.ui.grid_pagination.GridPaginationActivity"
            android:theme="@style/Theme.Adapters.FullScreen" />

        <activity
            android:name="com.vanskarner.adapters.ui.staggered_pagination.StaggeredPaginationActivity"
            android:theme="@style/Theme.Adapters.FullScreen" />

        <activity
            android:name="com.vanskarner.adapters.ui.simple_search.SimpleSearchActivity"
            android:theme="@style/Theme.Adapters.FullScreen" />

        <!--NEWS-->
        <activity
            android:name="com.vanskarner.adapters.examples.MenuActivity"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="com.vanskarner.adapters.examples.simple.SimpleActivity" />

    </application>

</manifest>